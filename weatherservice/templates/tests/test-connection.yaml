apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "weatherservice.fullname" . }}-test-connection"
  labels:
    {{- include "weatherservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "weatherservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
